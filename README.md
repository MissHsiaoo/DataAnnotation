
  # Display JSON Data Interface (Copy)

  This is a code bundle for Display JSON Data Interface (Copy). The original project is available at https://www.figma.com/design/7BVrw5MApJxq5HxvgxH7hq/Display-JSON-Data-Interface--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  